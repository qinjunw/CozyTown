# Cozy Town MVP 测试计划

## 1. 文档状态与范围

- 适用版本：可玩 MVP；M4 已完成；A1 自动接线已完成，等待人工 Scene-01 验收。
- 新增规划：T1 单镇扩建与 NPC 日常；其测试缝隙和验收行为见第 13 节，尚未实现或计入现有用例数量。
- Unity 版本：`6000.5.5f1`。
- 测试框架：`manifest.json` 请求 Unity Test Framework `1.4.5`，Unity `6000.5.5f1` 实际解析内置 `1.7.0`。
- 当前状态：两个 EditMode 测试程序集包含 279 个用例，PlayMode 测试程序集包含 35 个用例；2026-09-02 NPC 作者内容接线后的全量结果为 EditMode `279/279`、PlayMode `35/35`，均为 0 failed、0 skipped。
- 当前边界：M4 已自动化 schema v2 JSON 单槽存档、schema v1 确定迁移、五状态边界保存恢复与回滚、AI 代理编解码与故障回退、状态越权隔离和 Presenter 生命周期。A1 已完成 Production 资源门禁及 Scene-01a 至 Scene-01i；自动化已停在人工场景验收前。真实 AI 驱动 NPC、30 条评测、延迟与成本统计、Windows 构建和录屏在 Scene-01 验收后进入 M5。

本计划覆盖时间、背包、经济、商店、种植、畜牧、钓鱼、烹饪、NPC 对话、AI 对话边界和存档系统。框架阶段验证模块职责、公开契约、状态转换与跨模块闭环，不验证美术质量、数值平衡、复杂 NPC 日程、季节、天气、战斗或野外地图。

## 2. 测试目标

1. 每个模块可通过公开接口独立构造和验证，不依赖场景、MonoBehaviour 生命周期或真实网络。
2. 物品、金币、时间和生产状态只有明确的所属模块能够修改。
3. 跨模块操作满足原子性：失败时不扣除金币或材料，也不产生部分结果。
4. AI 仅生成受约束的对话结果，不直接修改背包、金币、农田、动物、任务或存档。
5. 存档往返后恢复可持久化状态；不兼容或损坏数据产生可识别结果，而不是静默生成部分状态。
6. 一条从购买到生产、烹饪、出售、进入下一天并重新读取存档的经济闭环可以复现。

## 3. 测试项目结构

测试程序集按被测层分别引用 Runtime 或 Unity 适配程序集，并统一使用 Unity Test Framework。下列树列出目录和代表文件，不是完整文件清单：

```text
Assets/CozyTown/Tests/
├── EditMode/
│   ├── CozyTown.Tests.EditMode.asmdef
│   ├── Application/
│   ├── Content/
│   ├── Core/
│   ├── Time/
│   ├── Inventory/
│   ├── Economy/
│   ├── Farming/
│   ├── Livestock/
│   ├── Fishing/
│   ├── Cooking/
│   ├── Npc/
│   └── Save/
├── UnityEditMode/
│   ├── CozyTown.Tests.UnityEditMode.asmdef
│   ├── CozyTownHudStateTests.cs
│   ├── DevelopmentSceneEditModeTests.cs
│   ├── GameplayDebugViewsEditModeTests.cs
│   ├── InteractionContextTests.cs
│   ├── NpcDebugViewEditModeTests.cs
│   ├── PlayerModalInputGate2DTests.cs
│   ├── PlayerMovement2DTests.cs
│   ├── ProxyNpcDialogueJsonCodecTests.cs
│   ├── SaveDebugViewEditModeTests.cs
│   ├── ShopDebugViewEditModeTests.cs
│   └── TownInteractionPointEventEditModeTests.cs
└── PlayMode/
    ├── CozyTown.Tests.PlayMode.asmdef
    ├── DevelopmentScenePlayModeTests.cs
    ├── FarmPresenterLateBindingPlayModeTests.cs
    ├── InteractionPlayModeTests.cs
    ├── PlayerModalInputGate2DPlayModeTests.cs
    ├── PlayerMovementPlayModeTests.cs
    ├── PlayModeTestComponents.cs
    ├── NpcDebugPresenterPlayModeTests.cs
    ├── SaveDebugPresenterPlayModeTests.cs
    ├── ShopDebugPresenterPlayModeTests.cs
    └── TownInteractionPointPlayModeTests.cs
```

`Assets/CozyTown/Tests/PlayMode/` 覆盖玩家位移、边界阻挡、输入边沿、组件禁用、碰撞目标选择、提示与反馈、模态输入互斥、Presenter 启停与晚注册，以及正式场景的完整生产经济闭环。

### 3.1 测试夹具

纯逻辑和组件测试使用测试内构造的最小定义，例如：

- 物品：土豆种子、土豆、鱼、鸡蛋、盐、烤土豆。
- 配方：一个有效配方和一个材料不足配方。
- 商店：一个可购买条目和一个可出售条目。
- 农田：一块可种植地块，作物成熟天数固定。
- 动物：一只鸡，喂食后次日产生一个鸡蛋。
- NPC：一个固定身份、固定回退文本的测试 NPC。

正式场景另保留 EditMode 资产测试和 PlayMode 闭环测试，显式读取 `CozyTown_Dev.unity`。资产测试验证七种交互语义、10 个物理交互实体、Visual 层级、碰撞、组件引用和窄接口装配；PlayMode 测试使用默认内容对象图，验证移动、输入恢复、4 名独立世界 NPC、存档入口，以及购买、种植、畜牧、钓鱼、跨日、烹饪、出售和再次投入。

### 3.2 测试替身

当前测试按端口提供最小替身：

- AI 客户端替身：返回成功、超时、空结果、非法标签或异常。
- 存档与模块替身：模拟空槽、保存失败和恢复失败，并验证成功回滚到调用前状态。
- 文件测试在测试独占的临时目录中运行，结束时清理，不访问应用持久化目录。

测试替身不得复制业务规则；业务判断仍由被测模块完成。

## 4. 测试层次

| 层次 | 运行方式 | 主要对象 | 框架阶段要求 |
|---|---|---|---|
| 纯逻辑组件测试 | EditMode | 值对象、服务、状态转换、校验器 | 必须 |
| 模块契约测试 | EditMode | 调用方与模块公开接口之间的输入、输出和失败语义 | 必须 |
| 跨模块集成测试 | EditMode | 背包、经济、生产、烹饪、存档组合 | 必须 |
| 场景与交互测试 | PlayMode | 输入、碰撞、HUD、生产/对话/存档面板和场景对象连接 | M4 已实现 |
| 人工可玩验证 | Unity Editor | 一座小镇内的完整用户路径 | 自动化闭环已完成；作品集发布前仍需人工验收 |

EditMode 测试不得调用真实大模型或真实网络接口。文件适配器测试只使用测试独占的临时目录；批处理场景注入内存存储，不访问玩家正式存档。真实 AI 服务只在受控的人工验证或独立后端集成环境中检查。

## 5. 系统契约

以下条目描述测试可观察的行为，不规定类名或内部实现。Runtime API 稳定后，测试名称应映射到这些契约。

### 5.1 时间系统

- 日期只能通过明确的“进入下一天”操作递增一次。
- 日期推进发布一次可订阅事件或返回一次等价结果；同一模块不得同时依赖两种通知路径重复处理。
- 日期推进触发生产模块结算时，每个订阅模块每一天只结算一次。
- 无效日期或重复载入不得使日期倒退。

### 5.2 背包系统

- 增加正数量物品后，物品数量按输入增加。
- 移除数量不超过库存时成功，并扣除准确数量。
- 移除数量超过库存时失败，库存保持不变。
- 零数或负数输入被拒绝，库存保持不变。
- 未知物品标识不能产生可用库存记录。
- 背包只存数量和物品标识；价格、配方和生产规则由对应定义或模块拥有。

### 5.3 经济与商店系统

- 金币增加和扣除只接受正数量。
- 余额不足时购买失败，金币和背包均保持不变。
- 背包无法接收物品时购买失败，金币保持不变。
- 出售仅使用商店定义的售价；出售成功后同时扣除物品并增加金币。
- 未知商品、不可购买商品或不可出售物品返回明确失败结果。
- 同一次购买或出售只产生一次资产变更。

### 5.4 种植系统

- 空地可种植有效种子；非空地或无种子时操作失败且不消耗种子。
- 浇水只改变允许浇水的已种植地块状态。
- 只有满足成长条件的日期推进才增加成长进度。
- 未成熟作物不能收获；成熟作物收获后向背包增加定义产量并重置地块。
- 背包不能接收产物时，地块保持成熟状态，不丢失作物。

### 5.5 畜牧系统

- 饲料存在且动物当天未喂食时，喂食成功并扣除一份饲料。
- 重复喂食或饲料不足时失败，不额外扣除物品。
- 已喂食动物在下一天结算时产生一次产物；未喂食动物不产生。
- 产物未领取或背包无法接收时保留可领取状态。
- 日期推进后重置当日喂食状态，但不重复生成已结算产物。

### 5.6 钓鱼系统

- 有效钓鱼结果只产生定义表中的一种鱼及正数量。
- 失败结果不增加背包物品。
- 背包无法接收时不丢弃奖励，并返回可处理的失败结果。
- EditMode 测试向 `Catch(int roll)` 传入固定值，覆盖成功、失败和物种选择。

### 5.7 烹饪系统

- 已知配方且材料充足时，准确扣除所有材料并增加一份料理。
- 任一材料不足时失败，所有材料数量保持不变。
- 输出无法加入背包时失败，材料保持不变。
- 未知配方或零产量配方被拒绝。
- 配方只引用物品定义；配方服务不负责购买、种植、钓鱼或畜牧结算。

### 5.8 NPC 与对话系统

- NPC 定义包含稳定标识、显示名称、人设信息和固定回退文本。
- 对话上下文只读取允许字段：NPC 身份、当前日期或时段、玩家近期活动、代表性物品、熟悉度和关键记忆。
- 对话结果只包含展示文本及允许的表现标签；未知标签被拒绝或归一为安全默认值。
- 固定对话路径不依赖网络，始终可用。

### 5.9 AI 对话边界

- Unity 客户端通过抽象对话提供者请求结果，不保存服务端密钥。
- AI 成功且结构有效时显示生成文本。
- 超时、网络错误、服务异常、空文本、无效结构或非法表现标签均返回该 NPC 的固定回退文本。
- 回退结果不得修改金币、库存、地块、动物或日期。
- AI 返回的文本即使包含“赠送物品”“扣除金币”等内容，也只作为文本显示。
- 日志可区分成功、超时、结构无效和回退，但不得记录密钥或完整敏感请求头。

### 5.10 存档系统

- 存档包含明确的模式版本号。
- 往返序列化保持日期、金币、背包、农田、动物和必要对话状态等价。
- 运行时缓存、服务实例、事件订阅和密钥不进入存档。
- 缺失存档返回“无存档”结果，不伪装成读取成功。
- 损坏 JSON、未知版本和缺少必需字段返回可识别错误，不覆盖原文件。
- 保存过程先产生完整数据，再替换目标；失败时保留上一份有效存档。
- 同一模式版本内新增可选字段必须具有确定默认值。

## 6. EditMode 覆盖矩阵

| 编号 | 模块 | 最低测试主题 | 优先级 |
|---|---|---|---|
| TIME-01 | 时间 | 进入下一天只递增一次 | P0 |
| TIME-02 | 时间 | 日结通知不重复 | P0 |
| INV-01 | 背包 | 增加与移除成功 | P0 |
| INV-02 | 背包 | 库存不足时无部分扣除 | P0 |
| INV-03 | 背包 | 非正数量和未知物品被拒绝 | P1 |
| ECO-01 | 经济 | 余额不足购买保持原状态 | P0 |
| ECO-02 | 商店 | 购买同时扣金币并加物品 | P0 |
| ECO-03 | 商店 | 出售同时扣物品并加金币 | P0 |
| FARM-01 | 种植 | 播种、浇水、成长、成熟状态转换 | P0 |
| FARM-02 | 种植 | 非法操作不消耗种子或作物 | P0 |
| FARM-03 | 种植 | 收获失败时保留成熟作物 | P1 |
| LIVE-01 | 畜牧 | 喂食后次日产物只生成一次 | P0 |
| LIVE-02 | 畜牧 | 重复喂食不重复扣料 | P0 |
| FISH-01 | 钓鱼 | 固定成功和失败结果 | P1 |
| FISH-02 | 钓鱼 | 奖励接收失败时不丢失 | P1 |
| COOK-01 | 烹饪 | 材料充足时原子转换 | P0 |
| COOK-02 | 烹饪 | 任一材料不足时全部状态不变 | P0 |
| DLG-01 | 对话 | 合法 AI 结果通过 | P1 |
| DLG-02 | 对话 | 六类异常统一落入 NPC 固定回退 | P0 |
| DLG-03 | 对话 | AI 结果不能触发游戏状态修改 | P0 |
| SAVE-01 | 存档 | MVP 状态往返等价 | P0 |
| SAVE-02 | 存档 | 缺失、损坏和未知版本可区分 | P0 |
| SAVE-03 | 存档 | 新增可选字段采用确定默认值 | P1 |
| LOOP-01 | 集成 | 购买至出售的经济闭环守恒 | P0 |
| LOOP-02 | 集成 | 闭环状态保存并恢复 | P0 |

覆盖矩阵描述完整 MVP 的最低回归范围。每个里程碑必须自动化其已实现范围内的 P0；尚无对应适配器的 P0 保留为后续里程碑入口条件。P1 可以随对应交互实现补齐，但公开失败语义必须先确定。

当前全量回归包含 279 个 EditMode 和 35 个 PlayMode 用例，覆盖组合根、时间溢出、背包容量、稳定角色/商店经济状态存储、显式角色与商店身份的守恒购买和出售、库存感知的购买/出售投影、交易与生产模块回滚、连续跨日、默认内容可达性、配置集合隔离、NPC 作者资产加载与不可变 Catalog 投影、schema v2 文件往返、schema v1 确定迁移、原文件保护、五状态边界存档回滚、AI 故障回退和状态越权隔离、A1 Production 资源契约，以及 Unity 像素相机、Tilemap、Production 场景引用、玩家动画、角色 `24×32` 图集及其包络和左右镜像轮廓、建筑屋顶前景、农田和母鸡状态、真实 Physics2D 边界、HUD、七种交互语义、10 个物理交互实体、模态输入生命周期、晚注册、4 名独立世界 NPC、池塘四向交互优先级、存档入口、Production UGUI 按钮接线、三档静态参考坐标边界和正式场景完整闭环。

## 7. 跨模块经济闭环场景

### LOOP-01：购买、生产、烹饪与出售

前置数据使用测试固定值，价格和产量不依赖正式平衡表。

1. 创建第 1 天、固定初始金币和空背包。
2. 从商店购买一份土豆种子和一份盐。
3. 断言金币减少量等于两项买价之和，背包各增加一份。
4. 在空地播种，断言种子减少一份。
5. 浇水并推进到配方所需作物成熟日；每天只结算一次成长。
6. 收获土豆，断言地块重置且背包获得定义产量。
7. 制作烤土豆，断言土豆和盐按配方扣除，料理增加一份。
8. 向商店出售料理，断言料理减少一份，金币增加定义售价。
9. 使用出售所得再次购买一份生产资料，断言背包取得下一轮投入。
10. 断言最终金币等于“初始金币 - 首轮买入成本 + 出售收入 - 再投入成本”，且任何失败步骤均未留下部分资产变更。

### LOOP-02：日结、畜牧和存档恢复

1. 购买或预置一份鸡饲料，给鸡喂食。
2. 保存第 1 天状态并执行存档往返读取。
3. 从读取结果进入下一天，断言日期只增加一次并生成一个可领取鸡蛋。
4. 领取鸡蛋，再次调用同一天结算入口，断言不产生第二个鸡蛋。
5. 保存并读取，断言日期、金币、背包、喂食状态和产物状态等价。

### LOOP-03：钓鱼食材进入配方

1. 向 `Catch(int roll)` 传入命中一种已知鱼的固定值。
2. 完成一次成功钓鱼，断言背包增加对应鱼。
3. 预置配方所需商店材料，完成烹饪。
4. 断言鱼和商店材料准确扣除，料理增加一份。

## 8. AI 失败与安全场景

每种失败输入必须单独测试，以便定位回退原因：

| 输入 | 预期结果 |
|---|---|
| 请求超时 | 返回当前 NPC 的固定回退文本，原因标记为超时 |
| 网络异常 | 返回固定回退文本，原因标记为传输失败 |
| 提供者抛出异常 | 异常被边界捕获，返回固定回退文本 |
| 空文本 | 判定无效并回退 |
| 缺少必需字段或 JSON 无法解析 | 判定结构无效并回退 |
| 未知表现标签 | 拒绝标签或替换为安全默认值；文本处理遵循已确定契约 |
| 文本声称修改资产 | 只展示文本，所有游戏状态保持不变 |

若实际 Runtime 将解析、校验和回退拆为不同组件，应分别测试各组件，并保留一条从提供者到最终对话结果的集成测试。

## 9. 存档兼容策略测试

当前写入模式为 `v2`，并保留固定的 `v1` 迁移夹具；实际常量名称以 Runtime API 为准。

- `v2 -> v2`：世界种子、全部角色与商店状态、时间、农田和畜牧完整往返等价。
- `v1 -> v2`：顶层玩家资产迁入默认角色，并按固定 legacy seed 生成默认商店当日状态；重复加载结果相同且不改写原文件。
- `v1` 缺少必需字段：加载失败并返回字段错误。
- `v0` 或未来版本：返回不支持版本，不尝试猜测字段或降级写回。
- 重复主体、非法物品、负数量或跨模块日期错位：加载失败且运行时状态不变。
- 损坏内容：返回解析错误，原始存档保持不变。
- 保存中断：上一份有效存档仍可读取。

后续增加模式版本时，必须先新增上一版本的不可变夹具和逐版本迁移测试，再修改读取路径；不得用当前写入结果覆盖历史夹具。

## 10. 自动化执行与当前结果

### 10.1 执行命令

在 PowerShell 中从项目根目录执行：

```powershell
& '<unity-editor>/Editor/Unity.exe' `
  -batchmode `
  -nographics `
  -projectPath '<project-root>' `
  -runTests `
  -testPlatform EditMode `
  -testResults '<project-root>/Logs/EditModeTests.xml' `
  -logFile '<project-root>/Logs/EditModeTests.log'
```

运行 PlayMode 时，将 `-testPlatform` 改为 `PlayMode`，并把结果和日志文件名改为 `PlayModeTests.xml`、`PlayModeTests.log`，避免覆盖 EditMode 证据。

命令不附加 `-quit`，由 Unity Test Framework 在测试结束后关闭批处理进程。附加 `-quit` 可能在测试运行器写出 XML 前结束 Editor。

验证结果必须同时满足：

1. 对应平台的 XML 存在且本次运行时间匹配。
2. XML 中失败数为 `0`。
3. 对应平台的日志不包含编译错误、未激活许可证或测试运行器异常。

Windows 上 Unity 启动器可能先返回退出码 `0`，而编辑器进程随后在日志中报告失败。因此不得只使用 PowerShell 的 `$LASTEXITCODE` 作为通过依据。

### 10.2 M3 验证结果

2026-08-29 在包含完整 M3 改动的隔离 Git worktree 中运行 Unity `6000.5.5f1`。`M3UnityEditModeTests.xml` 记录 108 passed、0 failed、0 skipped；`M3UnityPlayModeTests.xml` 记录 22 passed、0 failed、0 skipped。PlayMode 的正式场景用例通过真实 `PlayerInteractor2D` 完成购买、种植、喂鸡、固定鲤鱼、两次跨日、收蛋、收获、两道料理、四次成功出售和再次购买；金币从 300 变为 260、405，最终为 385。两份日志中 `error CS`、`Scripts have compiler errors`、`Compilation failed`、`Test run failed`、未处理异常和运行态装配错误的匹配数均为 0。

### 10.3 M4 验证结果

2026-08-29 在集成提交 `47332fd` 上运行 Unity `6000.5.5f1`。`M4EditModeTests.xml` 记录 151 passed、0 failed、0 skipped；`M4PlayModeTests.xml` 记录 26 passed、0 failed、0 skipped。新增用例覆盖临时文件写入和实际替换失败时保留旧存档、截断与有类型但无效 JSON、版本与载荷错误区分、五模块恢复失败回滚、AI 超时/异常/空结果/非法标签回退、恶意文本不改变五类状态、异步 Presenter 停止后不更新 UI，以及正式场景 4 名 NPC 和存档面板装配。两份日志中 `error CS`、`Scripts have compiler errors`、`Compilation failed`、`Test run failed`、`Unhandled Exception` 和 `NullReferenceException` 的匹配数均为 0，PlayMode 日志的 Bootstrap 初始化失败匹配数为 0。

### 10.4 A1 Production 美术包验证结果

2026-08-29 使用 Unity `6000.5.5f1` 在最终 A1 批次上运行测试。`A1HardBlockersFullEditModeTests.xml` 记录 158 passed、0 failed、0 skipped；`A1HardBlockersFullPlayModeTests.xml` 记录 26 passed、0 failed、0 skipped。`ProductionPixelArtAssetManifestTests` 验证 11 个 PNG、98 个 Sprite、32 色成员、二值 Alpha、导入参数、切片、Pivot、4× 最近邻预览、道路边缘连接、BottomCenter 脚底基线和 UI 九宫格 border。两份日志中编译错误、测试失败、未处理异常和 Bootstrap 初始化失败的匹配数均为 0。资源语义可读性由 `docs/ART_ACCEPTANCE.md` 的人工记录补充，不由自动化测试替代。

### 10.5 Scene-01 场景接线测试缝隙

- EditMode 场景资产测试通过公开的 `Camera`、`PixelPerfectCamera`、`Grid`、`Tilemap`、`SpriteRenderer` 和表现组件验证 Production 引用，不读取私有字段或运行时 `AssetDatabase` 查找结果。
- Scene-01c 只通过 UGUI 的公开 `Canvas`、`CanvasScaler`、`Image`、`Button`、`Text` 组件，现有 View 的 `Render`、`Show`、`Hide`、`Request*` 接口，以及 `Button.onClick` 验证界面。测试不调用 `OnGUI`，不读取私有字段，也不依赖仅供布局使用的子对象层级。
- PlayMode 测试通过真实玩家移动、交互输入和既有 Presenter 入口验证四方向动画、状态刷新和 7 个交互点，不重复测试领域算法。
- 资源导入测试验证现行 13 个 PNG、106 个 Sprite、主角帧包络和左右镜像轮廓；场景测试只验证哪些资源被消费以及显示配置，不复制清单算法。
- 每个垂直切片先记录能精确描述缺失能力的 RED，再加入通过该测试的最小接线；固定 NPC 回退和完整经济闭环作为不回归基线。

### 10.6 Scene-01a/01b 验证结果

2026-08-29 在正式场景完成世界与运行时状态接线后运行 Unity `6000.5.5f1`。`Scene01bCommittedSceneEditModeTests.xml` 记录 159 passed、0 failed、0 skipped；`Scene01bCommittedScenePlayModeTests.xml` 记录 26 passed、0 failed、0 skipped。场景测试验证 `16 PPU`、`320×180` Pixel Perfect Camera、Tilemap、7 个 Production 功能点、玩家移动与停止 Sprite、6 个农田土壤/作物叠层，以及母鸡 idle/fed/product-ready 状态。完整 PlayMode 经济闭环和固定 NPC 回退继续通过。A1 场景升级入口连续执行两次后，`CozyTown_Dev.unity` 的两次 SHA-256 比较结果相同；哈希只用于本次幂等性检查，不作为跨版本场景身份。

### 10.7 Scene-01c 自动化停止线

Scene-01c 按画布与皮肤、HUD 与交互提示、存档、首个商店模态、其余模态和场景回归的顺序逐片执行 RED→GREEN。自动化验证 `320×180` 参考分辨率、Production 九宫格与图标引用、文字值、按钮状态与事件接线、关键 `RectTransform` 未越界、模态输入恢复及既有经济闭环；测试不重复领域规则。

EditMode 与 PlayMode 全量通过、场景无丢失引用或运行时异常、升级入口连续两次结果一致后，Scene-01c 自动化停止。文字肉眼可读性、图标辨识、Tile 接缝、玩家脚底稳定、整体构图及 `320×180`、`640×360`、`1280×720` 实际画面质量由人工场景验收判定，不使用截图黄金图替代。人工场景验收通过前只保留固定 NPC 对话与故障替身，不调用真实 AI 服务，也不开始 AI NPC 评测。

### 10.8 Scene-01c 验证结果

2026-08-29 使用 Unity `6000.5.5f1` 完成 Scene-01c 自动化。TDD 垂直切片依次接入画布与皮肤、HUD、交互提示、存档、商店、农田、床、鸡舍、池塘、厨房和 NPC 固定回退界面；所有模态均使用 UGUI，退役的 IMGUI 绘制路径已删除。场景级 PlayMode 用例通过真实 `Button.onClick` 执行保存、读取、购买和关闭，并验证正式商店、鸡舍和厨房视图禁用/重启时的面板与监听生命周期；EditMode 用例对 10 个正式面板执行三档同宽高比的静态参考坐标边界检查，实际分辨率布局仍由人工验证。`Scene01cPostReviewFullEditMode.xml` 记录 166 passed、0 failed、0 skipped；`Scene01cPostReviewFinalPlayMode.xml` 记录 27 passed、0 failed、0 skipped，相关日志未匹配到编译错误、测试失败或未处理异常。修正 Unity Text 首次序列化字号边界后，连续执行场景升级器的 `CozyTown_Dev.unity` SHA-256 保持一致。自动化现已停止，下一步仅执行第 11 节人工场景验收；真实 AI 服务和 AI NPC 评测仍未启用。

2026-08-30 的首次人工场景检查发现鸡舍、池塘和厨房显示为洋红块。新增场景契约测试先复现 `Coop` 直属 `Visual` 的材质为空，再由 A1 世界视觉升级器为全部交互视觉显式绑定 URP `Sprite-Lit-Default`，并更新正式场景中的三个空引用。目标测试为 1 passed，正式场景 EditMode 子集为 5 passed；全量结果为 EditMode 167 passed、PlayMode 27 passed，均为 0 failed、0 skipped。升级器重复执行前后的场景 SHA-256 一致。自动化结果只确认材质引用和回归状态，鸡舍、池塘与厨房的实际显示仍需再次人工验收。

同日的第二次人工检查发现 HUD、存档和对话模态的背景透出世界场景。资产契约测试先复现 `ui_panel` 九宫格可拉伸中心包含 64 个透明像素，再由 A1 批次定义只为该单元填充 `#1F1B24FF` 并重建 Production PNG 与 4× 预览。目标测试为 1 passed，Production 资源清单为 4 passed；全量结果为 EditMode 168 passed、PlayMode 27 passed，均为 0 failed、0 skipped。其余 11 个 UI 单元、另外 10 张 Production PNG、Meta 文件和场景均未变化；A1 重建与场景升级的重复执行结果保持一致。自动化结果不替代三档实际画面的人工复查。

同日的第三次人工检查发现 `ui_panel` 四角保留绿色、米色和深棕拼块，九宫格放大后在 HUD 与模态面板重复形成花色。新增视觉参考固定近黑纯底与直线木框方向，资产契约测试先逐像素复现原面板偏差，再由 A1 批次定义生成三层暖棕内缩色带和纯色中心。目标测试为 1 passed，Production 资源清单为 5 passed；全量结果为 EditMode 169 passed、PlayMode 27 passed，均为 0 failed、0 skipped。两次 A1 Build 的 22 个输出哈希一致；仅首个 UI 单元及其 4× 预览对应区域变化，其他 Production、预览、Meta 和场景均未变化。三档实际画面仍需人工复查。

### 10.9 Scene-01d 常驻 UI 重构

Scene-01d 采用四个独立 RED→GREEN 垂直切片：常驻壳、交互气泡、包裹与快捷栏、系统菜单。该切片只重组正式 UI、增加只读投影和输入路由，不修改背包写规则、存档 schema、交互点数量、玩法结果或 AI 边界。

- 常驻壳通过公开 `RectTransform`、`Image`、`Button` 和 `Text` 验证左上 HUD、右上齿轮、底部五格快捷栏及旧左下交互面板退役；测试不依赖装饰性子节点顺序。
- 交互气泡通过 `PlayerInteractor2D` 的公开提示锚点和 View 显示状态验证最近目标、离开范围及模态停用；不读取 Collider 集合或私有目标字段。
- 包裹通过 Runtime 只读投影验证配置顺序、堆叠拆分、空槽和容量；Unity View 只显示图标、数量与空槽。B 与 `1` 至 `5` 使用 Input Action 公开绑定和可注入输入边沿验证。
- 系统菜单通过 Gear、保存、读取、设置、返回和离开按钮的 `onClick` 与公开事件验证。自动化使用退出替身，不调用真实 `Application.Quit`。
- 包裹、系统菜单和既有业务模态通过 `PlayerModalInputGate2D` 验证两两互斥、关闭恢复、撤销恢复与组件停用恢复。
- `ui_icon_settings` 与气泡化的 `ui_marker_interact` 继续满足 16 PPU、Point、无压缩、二值 Alpha、32 色成员和 4× 最近邻预览；现有图集只允许目标格发生像素变化。
- 最终重新运行完整 EditMode、PlayMode、Production 清单、场景幂等和经济闭环。自动化结果不替代三档分辨率下的人工可读性与遮挡检查。

2026-08-30 使用 Unity `6000.5.5f1` 完成 Scene-01d 自动化。组件测试覆盖只读投影、槽位渲染、系统菜单按钮与模态门生命周期；场景 PlayMode 使用虚拟 Keyboard 经过正式 `PlayerInput`、`InputSystemPlayerInputSource` 和 `CozyTownInventoryPresenter` 验证数字 `5` 选中第五格以及 `B` 打开、关闭包裹。场景级 EditMode `5/5`、PlayMode `2/2` 通过；全量结果为 EditMode `179/179`、PlayMode `31/31`，均为 0 failed、0 skipped。场景升级器连续两次运行后的 SHA-256 均为 `6A3420F637BA43A8A0F30EB8411C9008107DA737EE031E1379A595112AD4287F`。自动化不替代三档实际画面的人工检查。

### 10.10 Scene-01e 碰撞、NPC 世界实体与 UI 可读性

本切片先以公开组件和行为建立三组 RED：系统菜单主按钮对比度与无底框灰色齿轮；建筑、农田和池塘缺少非 Trigger 碰撞及门口/岸线 Trigger；场景只有一个 NPC 世界点且一次显示四人。GREEN 只允许加入以下结果：

- 四座建筑使用保留门槽的实体碰撞，农田与池塘使用与可见外沿相符的实体碰撞；实体障碍不放在交互点父链上。
- 建筑业务交互只在门口出现；池塘从可达岸边一圈出现；玩家的真实 Rigidbody2D 不能越过对应边界。
- Mina、Eli、Ren 和 Sora 使用四个稳定 ID、四个世界 Sprite 和四个不同合法坐标；每个交互只产生对应 NPC 的一次对话请求。
- 系统主按钮和右上齿轮满足 ADR-0008 的颜色、字号、Sprite 与层级契约。

目标测试通过后运行全量 EditMode、PlayMode及升级幂等检查。测试记录只填写实际执行结果；人工复核继续覆盖门口进入深度、池塘岸线贴合、NPC 构图和三档分辨率下的文字辨识。

2026-08-31 使用 Unity `6000.5.5f1` 在隔离项目副本中完成 Scene-01e TDD。RED 运行发现 183 个 EditMode 用例，其中 176 passed、7 failed；失败分别命中旧齿轮、旧按钮、缺少障碍、单一 NPC 世界入口和缺少四人世界图集。首次最小副本未包含预览目录，因此 Production 清单的失败文本同时包含预览缺失；完整复制 `ArtSource` 后不再出现该环境噪声。独立 review 随后新增池塘四向真实 Probe 用例，RED 精确复现左岸被 Ren 对话入口抢占，移动 Ren 后转为 GREEN。最终全量结果为 EditMode `183/183`、PlayMode `35/35`，均为 0 failed、0 skipped；日志未匹配到编译错误、测试失败、未处理异常或空引用。A1 世界与 Production UI 升级入口连续执行后的场景 SHA-256 均为 `C735E2022D58B0AA2F6F58E7DE402DC5E372D0816F6A1B1F72A7386FA0EE3552`。这些结果关闭自动化门禁，不替代人工场景验收。

### 10.11 Scene-01f UI 遮挡、房屋背面和角色表现收口

本切片继续使用场景公开组件与 Physics2D 作为 seam。首轮 RED 共 185 个 EditMode 用例，结果为 182 passed、3 failed：业务模态 sibling 位于快捷栏之前、房屋实体碰撞仍覆盖完整 4.0 世界单位高度、母鸡位于鸡舍障碍内。后续两个 UI 竖切分别以目标测试复现业务按钮仍为白色，以及快捷栏数字仍为奶油色。GREEN 只改变七个业务模态的 sibling 顺序和按钮主题、五个快捷栏数字颜色、建筑实体上边界、母鸡坐标和四个 authored NPC 世界 Sprite；领域规则、交互语义、Presenter 接口和存档保持不变。

2026-08-31 使用 Unity `6000.5.5f1` 在隔离项目副本中完成最终回归。EditMode `188/188`、PlayMode `35/35` 通过，均为 0 failed、0 skipped；两份日志未匹配到编译错误、测试失败、未处理异常、空引用或崩溃。A1 世界与 Production UI 升级入口再次执行前后的场景 SHA-256 均为 `2E14EC0AA0D49EA94E0E03B780A7E2AAD3942B5FBEAB8781D414A50D0A8178B9`，主工作区场景与隔离场景哈希相同。自动化验证业务模态位于快捷栏之后、颜色契约、房屋下方 3/5 阻挡与上方 2/5 可通行、母鸡位于农田旁草地、四名 NPC 的 ID→世界 Sprite→头像映射及最小 3.0 世界单位间距。屋后透视遮挡、三档实际画面和最终构图仍由人工验收。

### 10.12 Scene-01g 屋顶前景与角色辨识度

本切片以场景公开 `SpriteRenderer`、Production Sprite 像素和稳定 NPC 映射作为 seam。屋顶 RED 命中四座房屋缺少前景渲染器；NPC RED 命中世界 Sprite 未保留足够头像色板特征；主角 RED 命中场景仍引用 `16×24` 图集。GREEN 只增加确定性屋顶派生图集、升级主角和 NPC 世界图集，并更新正式场景引用。

屋顶测试验证每个 `64×64` 前景单元底部 38 行全透明、顶部 26 行与建筑底图像素一致，且其排序值高于玩家、建筑底图排序值低于玩家。角色测试验证主角 12 帧均为 `24×32`，四名 NPC 均为完整身体轮廓，并保持稳定 ID→世界 Sprite→头像映射和至少 70% 的头像不透明色板覆盖。

2026-09-01 使用 Unity `6000.5.5f1` 完成回归。全量 EditMode `190/190`、PlayMode `35/35` 通过，均为 0 failed、0 skipped；日志未匹配到 C# 编译错误、测试失败、未处理异常、空引用或崩溃。再次执行完整 A1 编译与场景升级后，场景、屋顶、NPC 和主角输出的 SHA-256 分别保持为 `F86DE41AE2CEDAF3EF9FBFFAD444AEA6E8D92DF35889F04B03A30D853673F552`、`40B45ED71F6BD6207E9397602C308310FA03B3E4C84CAED33128A9B955CEACD2`、`75AED52828098D0449CF4598A0C1E6CF5A7186E376D5C24317933DB210951221` 和 `CE56459B5F09EAB771EA7E16220F024DDD60DA452C8517257356E4E0B05F7997`。自动化不替代三档实际画面的屋后遮挡和角色比例检查。

### 10.13 Scene-01h 门槽进入深度

本切片继续使用公开场景组件和真实 Physics2D 行为作为 seam。EditMode 测试从建筑非 Trigger `PolygonCollider2D` 推导门槽后沿，从玩家 `24×32` Sprite 边界推导头顶位置，并读取屋顶前景最低不透明像素；验收要求玩家位于最深可达位置时，头顶仍低于遮挡起点至少 `1 px`。PlayMode 测试保留四座房屋的真实 `Rigidbody2D` 进入和侧墙阻挡验证。

RED 中商店样本的最深玩家头顶为 `3.95`，允许上限为 `3.3125`。GREEN 只把四座房屋的门槽深度从 `1.25` 或 `1.30` 世界单位统一调整为 `0.6`；门宽、门口业务 Trigger、非门墙体、屋后通行区和渲染顺序均未改变。目标 EditMode `1/1` 与目标 PlayMode `1/1` 通过。

2026-09-01 使用 Unity `6000.5.5f1` 在隔离项目副本中完成全量回归。EditMode `191/191`、PlayMode `35/35` 通过，均为 0 failed、0 skipped；日志未匹配到 C# 编译错误、测试失败、未处理异常、空引用或崩溃。场景升级入口连续执行前后的场景 SHA-256 均为 `B46F9F6E41E99E131D69798CA7E3CC2B425A56CF6A3F2FB23B525446FB20DB27`。自动化不替代三档实际画面中的进入手感和头部留白检查。

### 10.14 Scene-01i 主角移动图集规格化

本切片以 Production PNG 的公开像素和既有 Sprite 导入结果作为测试缝隙。12 个 `ArtSource/Authored/A1/Characters/Player/*.pixels` 单元必须为精确 `24×32`；正式输出路径、12 个 Sprite 名、`16 PPU`、BottomCenter Pivot 和 Animator 映射保持不变。包络测试要求脚底 `y=0`、顶部 `y=29` 或 `y=30`、水平范围不越过 `x=2..21`、中心位于 `x=11..12`、脚底至少 2 个不透明像素、所有不透明像素通过四邻域连接到脚底，且 12 帧可见高度差不超过 1 像素。左右三个对应帧的整体水平镜像轮廓交并比必须不低于 `0.9`，底部 8 行必须不低于 `0.85`。

RED 先复现旧 Production 帧的可见高度分布为 `29..32`，并命中右向帧脚部缺失；左右 idle、walkA、walkB 的镜像轮廓交并比分别为 `0.791`、`0.639`、`0.740`。GREEN 将 12 个精确 authored 单元接入既有 A1 Catalog；Production 目标夹具 `9/9`、全量 EditMode `193/193`、PlayMode `35/35` 通过，均为 0 failed、0 skipped。

连续 A1 重建前后的主角 Production PNG、4× 预览和当前场景 SHA-256 分别保持为 `6B3FDBE287AB23372CDAEBAAE41CAE645B9B4548EE2A2F15B175E7E9C1C13D90`、`1717079B2C7AE1D716959E45533788D86AFD9A508024CCE48F62DC73312E1604`、`903D7E53BCFB4D9244939F3F08EBF4C2499FB352520C06CB29AA9C2AB3BBEECC`。自动化不替代三档实际画面中的体型一致性、脚部完整性和步行动作观感检查。

### 10.15 发布与 AI 代理配置

项目配置测试验证唯一启用的构建场景为 `CozyTown_Dev.unity`，Standalone Player 使用 `Kaiisa`、`KaiisaTown` 和 `com.Kaiisa.KaiisaTown`。AI 代理配置测试验证进程环境变量覆盖场景默认值、未配置时保留固定对话路径，并拒绝无法解析、小于 `0.1`、`NaN` 或无穷大的超时值。

2026-09-01 使用 Unity `6000.5.5f1` 运行全量回归。EditMode `201/201`、PlayMode `35/35` 通过，均为 0 failed、0 skipped；测试进程未设置 AI 代理环境变量，也未访问线上模型服务。

### 10.16 守恒型角色—商店经济状态存储首个切片

首个 RED 通过公开 `IEconomyStateStore` 用例要求一次有效提交同时发布角色背包/钱包和商店库存/钱包；Unity 编译器因缺少角色与商店快照类型而失败。GREEN 新增两个防御性复制的经济快照、稳定 ID 查询和内存配对提交，不修改旧商店、组合根、Unity 接线或存档 schema。

随后增加无效商店候选用例，验证提交返回 `economy.shop_invalid`，且角色与商店均保持提交前状态。2026-09-02 使用 Unity `6000.5.5f1` 运行目标 EditMode `2/2` 和全量 EditMode `203/203`，均为 0 failed、0 skipped；日志未匹配到 C# 编译错误、测试失败、未处理异常或空引用。本切片没有接入任何 Unity 对象或替换生产服务，因此没有重新运行 PlayMode；最近一次全量 PlayMode 结果仍为 `35/35`。

### 10.17 显式角色—商店身份的原子购买

购买切片先通过公开交易端口固定一个成功行为：输入商店、角色、物品和数量后，角色钱包与商店库存减少，角色背包与商店钱包按同一总价增加。RED 因显式身份交易端口及实现不存在而编译失败；GREEN 通过 `IEconomyStateStore.Commit` 一次发布两个候选快照。成功用例直接断言交易前后总货币为 `1100`、土豆种子总数量为 `3`，没有凭空创建或销毁资产。

失败矩阵覆盖未知商店、未知角色、未知物品、零数或负数、库存不足、角色资金不足、无效买价、背包容量不足和状态提交拒绝；每条路径均断言角色与商店保持调用前状态。2026-09-02 使用 Unity `6000.5.5f1` 运行该夹具 EditMode `11/11` 和全量 EditMode `214/214`，均为 0 failed、0 skipped。本切片没有替换旧 Unity 商店接线，因此 PlayMode 未重跑；最近一次全量 PlayMode 结果仍为 `35/35`。

### 10.18 显式角色—商店身份的原子出售

出售切片先固定成功行为：角色背包减少出售数量、商店库存增加同量物品，商店钱包按回购总价扣款，角色钱包取得同额货币。RED 因显式身份交易端口没有 `Sell` 而编译失败；GREEN 通过同一个 `IEconomyStateStore.Commit` 发布角色与商店候选。成功用例断言交易前后总货币保持 `1100`、土豆种子总数量保持 `3`。

独立拒绝夹具覆盖未知商店、未知角色、未知物品、零数或负数、角色物品不足、商店资金不足、非正回购价和状态提交拒绝。目标成功用例 `1/1`、拒绝夹具 `9/9`、全量 EditMode `224/224` 通过，均为 0 failed、0 skipped。该切片仍未替换旧 Unity 商店接线，因此 PlayMode 未重跑；最近一次全量 PlayMode 结果仍为 `35/35`。

### 10.19 库存感知的商店交易投影

投影查询显式接收商店与角色 ID，并在每次调用时读取权威经济状态。购买列表只包含正买价且商店现货为正的报价；出售列表只包含正回购价且角色持有量为正的报价。两个列表按稳定物品 ID 排序，并分别携带当前数量和单价；投影同时返回角色与商店余额。

RED 在缺少 `ShopTradingViewState` 和显式身份查询方法时编译失败。GREEN 夹具覆盖无报价物品、售罄商品、单向报价、未知身份，以及成功购买和出售后的余额与数量刷新。目标 EditMode `4/4` 和全量 EditMode `228/228` 通过，均为 0 failed、0 skipped。旧 Unity 商店 Presenter 保持原接线，实际切换和旧接口移除留在 #21，因此本切片未重跑 PlayMode；最近一次全量 PlayMode 结果仍为 `35/35`。

### 10.20 确定性每日库存替换

补货策略只生成商店候选快照，不直接发布状态。版本一协议固定 FNV-1a 字节编码、xorshift32 随机序列、六条规则的遍历顺序和四类保底抽取；相同世界种子、商店 ID、日期与算法版本产生相同库存。同日调用保留交易后的实际库存，新日完整替换会清退角色售入商品，同时保持商店钱包不变。

首个 RED 在补货策略与规则类型缺失时编译失败；第二个 RED 锁定默认配置尚未暴露六条规则。GREEN 用例覆盖固定黄金向量、同日幂等、四类保底、重复调用确定性、日期倒退和未知算法版本，并复核默认内容引用。目标补货与内容夹具 `37/37`、全量 EditMode `236/236` 通过，均为 0 failed、0 skipped。该切片没有修改日切协调器或 Unity 接线，因此 PlayMode 未重跑；最近一次全量 PlayMode 结果仍为 `35/35`。

### 10.21 原子跨日补货发布

`IDayTransitionCoordinator` 继续作为唯一跨日入口。商店感知路径在任何模块变化前读取并校验商店日期、生成目标日候选，再推进时间、农田和畜牧，最后通过 `IEconomyStateStore.CommitShop` 发布商店候选；商店提交失败时恢复前三个参与者，商店因拒绝提交保持原状态。该保证适用于当前 Unity 主线程同步执行和现有服务可恢复自身快照的边界，不声明跨线程强事务。

首个 RED 在共享世界种子状态与商店感知构造入口缺失时编译失败；组合根 RED 随后锁定默认对象图必须暴露并发布同一经济状态。GREEN 用例覆盖确定性成功日切、补货准备失败、农田失败、商店提交拒绝、商店日期错位、独立商店提交，以及默认组合根接线。目标 EditMode `22/22`、全量 EditMode `244/244` 和全量 PlayMode `35/35` 通过，均为 0 failed、0 skipped。

### 10.22 经济存档 schema v2 与 v1 迁移

schema v2 保存世界种子、全部角色背包与钱包、全部商店实际库存与钱包、最后补货日和补货算法版本，并继续保存时间、农田与畜牧状态。生产组合根通过角色背包和钱包适配器让旧生产模块与存档系统读写同一个 `IEconomyStateStore`，不保留独立的玩家经济副本。

加载先解析并迁移完整候选，再校验并按世界种子、时间、经济状态、农田和畜牧顺序恢复；失败时恢复调用前的五份快照。手写 v1 夹具固定 legacy seed 为 `1`，在第 3 天生成 `seed.carrot:5`、`seed.tomato:4`、`feed.chicken:12` 和 `ingredient.flour:7`，两次加载结果相同且原文件字节不变。用例还覆盖 2 个角色与 2 个商店往返、重复角色、未知物品、负数量、日期错位、缺失分段、损坏 JSON、未来版本、临时写入失败及完整回滚。

目标存档、协调器、经济仓库和角色适配器 EditMode `38/38`、全量 EditMode `260/260`、全量 PlayMode `35/35` 通过，均为 0 failed、0 skipped。

### 10.23 Unity 商店稳定身份与库存投影接线

Presenter RED 将公开绑定端口切换为 `ICharacterShopTradingCoordinator`，并要求查询、购买和出售均携带 `shop.town.general` 与 `character.player`；旧 Presenter 因仍返回 `ShopViewState` 且缺少三参数绑定入口而编译失败。GREEN 后，Presenter 在每次交易后重新查询权威投影，View 顺序显示商店现货购买行与角色持有出售行，并分别使用角色余额和商店余额控制按钮可用性。

正式场景用例通过系统菜单保存第 1 天状态，购买一份鸡饲料后加载，再次打开商店核对角色余额恢复为 `300`、商店余额恢复为 `10000`、鸡饲料库存恢复到保存值且出售列表为空。默认确定性库存第 1 天不包含土豆种子和盐，因此生产闭环先正常跨日至第 2 天再购买这些投入品；测试没有修改世界种子或绕过现货校验。

替代覆盖通过后，删除旧 `IShopService`、`IShopTradingCoordinator`、对应实现及 11 条无限库存模型专用测试。Presenter 目标 PlayMode `7/7`、Unity View 目标 EditMode `2/2`、正式场景目标 PlayMode `1/1`、全量 EditMode `249/249` 和全量 PlayMode `35/35` 通过，均为 0 failed、0 skipped。

### 10.24 守恒经济与迁移回归

正式生产闭环在 3 次买入、3 次卖出和最后 1 次再买入之后分别读取 `character.player` 与 `shop.town.general` 的公开经济快照。每笔普通交易均核对角色与商店余额、双方持有的交易物品数量、固定金币总额 `10300`，以及双方全部物品合计映射在交易前后相同；种植、畜牧、钓鱼、烹饪和每日补货仍作为具名生产、转换或外部刷新边界，不与普通交易的守恒断言混为一体。本票对已经实现的交易行为增加回归刻画，没有制造 Runtime 失败；首次测试编译暴露了包装器结果类型与公开端口签名不一致，改为 `OperationResult<ShopReceipt>` 后通过，Runtime 未修改。

正式场景在保存前通过池塘 UI 获得 1 条可出售鲤鱼。保存后购买鸡饲料并卖出鲤鱼，出售投影不再包含鲤鱼；再从系统菜单加载并重新打开商店，出售投影恢复鲤鱼持有量 `1`，同时角色余额、商店余额和鸡饲料库存恢复到保存值。这条路径补充验证 schema v2 中角色背包通过 UI 保存和恢复。

生产闭环目标 EditMode `1/1`、迁移／补货／默认内容／交易组合 EditMode `70/70`、正式场景保存入口 PlayMode `1/1`、全量 EditMode `249/249` 和全量 PlayMode `35/35` 通过，均为 0 failed、0 skipped。第 11 节的 Scene-01 人工验收仍是独立的用户执行门禁，本切片没有将其标记为通过。

### 10.25 NPC 作者内容与不可变 Catalog 接线

NPC 文案继续使用现有单一 `CozyTownMvpContentAsset` 作者入口；资产加载同时转换全局回退和四名 NPC 的稳定 ID、显示名称、人设与专属回退。Runtime 的 `NpcContentCatalog.Create()` 统一校验空字段和重复 ID，并以防御性副本提供定义只读视图、按 ID 的对话上下文投影以及专属／全局回退解析。`MvpContentValidator` 委托该工厂执行 NPC 规则，协调器和固定回退生成器不再接收原始可变集合。

TDD 证据分为三个初始 RED：Catalog 类型缺失导致测试程序集编译失败；资产尚未暴露全局回退与 NPC 序列化字段时，14 个目标资产测试中 5 个失败；消费者构造函数仍接受原始集合时，迁移夹具编译失败。独立审查随后用第 15 个资产测试复现任意唯一 ID 可以替换代码稳定 ID 的缺口，结果为 `14 passed, 1 failed`；加入 canonical ID 集合校验后，该夹具为 `15/15`。第二次架构复核用纯 Runtime 测试复现相同配置在资产与组合入口的判定差异，结果为 `0/1`；把 canonical ID 不变量下沉到 `MvpContentValidator` 后为 `1/1`。其余 GREEN 结果为 Catalog 目标 EditMode `6/6`、协调器与回退生成器目标 EditMode `25/25`、AI 对话完整夹具 EditMode `22/22`。AI 的超时、传输失败、无效结构、空文本、非法 emotion 和非法 action 均返回当前 NPC 的专属回退。

2026-09-02 使用 Unity `6000.5.5f1` 运行全量回归。EditMode `279/279`、PlayMode `35/35` 通过，均为 0 failed、0 skipped。本切片未改变稳定 ID、AI 标签白名单、500 字文本限制、状态写隔离或存档 schema；Scene-01 人工验收仍是独立门禁。

## 11. 人工验证步骤

完整 MVP 演示候选完成后，按以下顺序执行并记录结果、Unity 版本、提交号和异常截图：

1. 从空存档启动，确认初始日期、金币和背包与配置一致。
2. 在商店购买种子、盐和饲料；分别验证余额充足与不足路径。
3. 完成播种、浇水、睡觉推进和成熟收获，确认一天只成长一次。
4. 给鸡喂食并进入下一天，确认只产生一次鸡蛋。
5. 完成一次钓鱼成功和一次失败，确认失败不产生物品。
6. 制作一个材料充足配方，再尝试一个材料不足配方。
7. 出售原料或料理，核对金币变化与商品配置一致。
8. 与 4 名 NPC 分别执行固定回退对话，确认头像、选中标记、文本和关闭行为。
9. 保存、退出并重新进入，核对日期、金币、背包、农田、鸡和必要对话状态。
10. 使用备份文件模拟损坏存档，确认游戏给出可识别错误且不覆盖原文件。

Scene-01 人工验收通过后，M5 再执行真实 AI 成功对话、断网回退和恶意资产指令隔离检查；这些步骤不属于当前人工场景验收。

## 12. 阶段退出条件

### 12.1 M1 核心循环基础

- Runtime、Unity 适配层和两个 EditMode 测试程序集编译通过。
- 默认内容的稳定 ID、引用与投入可达性校验通过。
- 跨日协调失败不会留下时间、农田或畜牧的部分提交。
- Unity 组件不公开完整服务集合，交互上下文没有领域写入口。
- Unity Test Runner 的 56 个现有用例全部通过，日志无编译、测试或许可证错误。
- M1 退出时，可玩场景、PlayMode、磁盘存档和线上 AI 明确保留为后续范围。

### 12.2 M2 小镇可行走切片

- `CozyTown_Dev.unity` 包含可见玩家、四面碰撞边界、相机、HUD 和四类浅交互点。
- EditMode 场景测试验证资产装配、四类枚举与非空提示；PlayMode 验证运行态装配、实际位移、边界阻挡、最近目标和连续输入边沿。
- 正式场景中的商店、NPC、床和农田均完成一次自动触发，提示、计数与反馈可观察。
- 完整服务容器未进入交互上下文或通用场景组件。
- Unity Test Runner 的 57 个 EditMode 与 8 个 PlayMode 用例全部通过，相关日志无编译、测试、装配或许可证错误。
- 生产经济 UI、磁盘存档和线上 AI 明确保留为 M3/M4 范围。

### 12.3 M3 生产经济闭环

- 商店与四个生产模块通过各自的窄应用入口执行，Unity Presenter 不取得钱包、背包、原始生产服务或完整服务集合。
- 交易、播种、收获、喂食、领取、钓鱼和烹饪覆盖前置拒绝及依赖变更后回滚；每个相关服务至少保留一条代表性回滚失败诊断用例。
- 连续两次跨日让作物按浇水日成长并让已喂食动物只产生一次待领取产物；重复、跳日和回滚路径有自动化结果。
- 正式场景包含七种交互语义和 10 个物理交互实体，可通过真实交互边沿完成购买、生产、烹饪、原料与料理成功出售和再次投入。
- 六个功能面板共用互斥输入门控；关闭、禁用、撤销、重复启停和跨帧晚注册均有 PlayMode 结果。
- Unity Test Runner 的 108 个 EditMode 与 22 个 PlayMode 用例全部通过，相关日志无编译、测试、未处理异常或装配错误。

### 12.4 M4 持久化与 AI

- 单槽 schema v2 JSON 存档覆盖世界种子、时间、全部角色与商店经济状态、农田和畜牧；schema v1 可确定迁移，保存失败保留上一份有效文件。
- 空槽、损坏 JSON、未来版本和无效载荷返回可区分结果，五状态边界恢复失败会回滚调用前状态。
- AI 输出只包含文本和允许表现标签；超时、传输错误、提供者异常、空文本、结构错误和非法标签均返回固定对话。
- 恶意对话文本不能改变钱包、背包、时间、农田或畜牧状态。
- 正式场景装配 4 名 NPC 和单槽存档面板；批处理测试不访问玩家正式存档。
- Unity Test Runner 的 151 个 EditMode 与 26 个 PlayMode 用例全部通过，相关日志无编译、测试、未处理异常或装配错误。

### 12.5 完整 MVP 进入作品集交付

- Runtime 与 EditMode 测试程序集编译通过。
- 覆盖矩阵中的 P0 测试已实现且全部通过。
- AI 异常回退测试不访问真实网络。
- `LOOP-01` 与 `LOOP-02` 通过，并验证失败操作无部分状态变更。
- 存档往返、损坏数据和未知版本路径均有自动化结果。
- 人工验证中尚未实现的功能路径被标记为“未实现”，没有记录为通过。

## 13. T1 单镇扩建与居民日常（待实施）

验收行为和候选公开缝隙统一维护在 [T1 规划第 8 节](TOWN_LIFE_PLAN.md#8-tdd-验收与交付顺序)，避免在多个矩阵中复制不同的时间与存档规则。实施前确认新增缝隙，再逐个行为执行 RED → GREEN；本轮只更新规划，没有新增或运行测试。

- EditMode：稳定住宅/地点引用、日程分钟边界、重复评估、日内封顶、暂停不补时、加载重建和失败不发布；明确 NPC 活动不产生经济/生产变化。
- Unity EditMode：扩大地图/相机边界、四户和原有功能点身份、路线与脚部占地校验、资产导入及场景升级幂等。
- PlayMode：实际四人往返、障碍阻挡、入屋隐藏及 Trigger 清理、移动中打开/关闭对话、模态和失焦暂停、成功睡觉/加载重建、相机跟随与前后排序。
- 原静态 NPC 间距、六障碍计数和固定坐标夹具只代表 A1 基线。修改目标场景后按批准的新布局与动态可达行为更新断言，不能简单删除碰撞和交互回归。
- 时间使用显式可控输入；美术按 Production 导入结果检查；只在真正外部边界使用替身，不读取私有计时器、路径列表或方法调用次数。
- 人工结果分别记录扩大版 Scene-01 回归与 Town-01，至少覆盖 `320×180`、`640×360`、`1280×720`，正常倍率下观察出门、职业停留和日落归家。

真实 AI 仍在联合场景门禁后另行验证；只读世界查询和记忆测试不得被日程测试替代。若改选精确位置持久化，先补充版本迁移方案与固定 v2 夹具，再增加生产字段。
