# Preserve recorded evidence bytes so manifest and checksum hashes survive checkout.
* -text whitespace=blank-at-eol,blank-at-eof,space-before-tab,cr-at-eol
# Raw settings diffs and console logs retain recorded trailing spaces.
*.diff -whitespace
*.log -whitespace
