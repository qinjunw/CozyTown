# CozyTown 纯像素美术验收标准

## 1. 验收范围

本标准覆盖纯像素资源从 A0 风格锚点到 A1 Unity 生产资源的准入。自动化检查验证文件、Sprite 布局和 Unity 导入结果；人工检查验证像素画的可读性、一致性和原创性。自动化通过不能替代视觉验收。

## 2. TDD 测试缝隙

本阶段只在以下公开缝隙编写自动化测试：

1. Unity 将 `Assets/CozyTown/Art/Production/` 下的 PNG 导入后，通过 `AssetImporter.GetAtPath` 可观察到统一的纯像素设置。
2. A0 像素管线探针通过 `ImageConversion.LoadImage` 和 `AssetDatabase.LoadAssetAtPath` 可观察到源 PNG 尺寸、Alpha、颜色数及实际 Sprite 输出。
3. 生产清单声明 Sprite Sheet 后，通过 `ImageConversion.LoadImage`、`AssetImporter.GetAtPath` 和 `AssetDatabase.LoadAllAssetsAtPath` 可观察到真实 PNG 画布、二值 Alpha、导入参数、切片数量、PPU、Pivot 和名称。
4. 每张生产 PNG 的 4× 预览必须与原图逐像素最近邻扩展结果一致。
5. 正式场景开始消费生产资源后，通过场景中的公开 Camera、SpriteRenderer 和 Tilemap 组件验证引用及 Pixel Perfect Camera。
6. Scene-01c/01d 通过公开的 UGUI `Canvas`、`CanvasScaler`、`RectTransform`、`Image`、`Button`、`Text` 组件、现有 View 接口和 `Button.onClick` 验证 Production UI 引用与交互接线。
7. Scene-01i 通过 Production 主角图集的公开像素验证统一身体包络、脚底连续性和左右镜像轮廓，不读取 Animator 私有字段或依赖截图黄金图。

测试不得读取 `.meta` YAML、断言 AssetPostprocessor 私有方法、调用 `OnGUI` 或读取 UI 私有字段。A0 风格锚点不进入 Sprite 布局测试，因为它们不是生产资源。

## 3. A0 自动检查

- 五个约定文件全部存在于 `Assets/CozyTown/Art/References/A0/`。
- 文件为 PNG，能够被 Unity 导入并读取。
- 文件名与 `ART_DIRECTION.md` 一致，不以显示名称替代稳定资源名。
- A0 不被正式场景、Prefab 或运行时代码引用。

## 4. A0 人工验收

| 编号 | 检查项 | 通过条件 |
| --- | --- | --- |
| ART-A0-01 | 纯像素媒介 | 边缘由一致大小的硬像素块组成；没有抗锯齿、模糊、景深或体积光 |
| ART-A0-02 | 视角与光向 | 所有场景与角色保持 3/4 俯视；主要光源来自左上方 |
| ART-A0-03 | 地标可读性 | 商店、床/住宅、农田、鸡舍、池塘和厨房不看文字也能区分 |
| ART-A0-04 | 角色区分 | 玩家与 4 名 NPC 在轮廓、发色或服装中至少有两项稳定差异 |
| ART-A0-05 | 玩法边界 | 图片只表现当前 MVP，不出现野外、战斗、季节或新增可交互系统 |
| ART-A0-06 | 风格一致 | 五张锚点的像素密度、描边、饱和度和材质处理没有明显批次漂移 |
| ART-A0-07 | 原创性 | 不包含现有游戏的可识别角色、地图、建筑、Logo 或图标复制 |
| ART-A0-08 | 小尺寸可读 | 角色、地标和代表图标在目标原生尺寸或整数倍显示时仍可辨认 |

任一检查不通过时，只针对失败项重新生成或编辑。A0 原有软渐变缺陷继续记录，但根据 2026-08-29 的范围决策，不再阻断满足 A1 硬技术门禁的全套资源批次。

## 5. A1 Unity 导入验收

`Assets/CozyTown/Art/Production/` 下的 PNG 必须满足：

- Texture Type：Sprite。
- Sprite Mode：按资产清单声明为 Single 或 Multiple。
- Pixels Per Unit：`16`。
- Filter Mode：Point。
- Compression：None / Uncompressed。
- Generate Mip Maps：Off。
- Wrap Mode：Clamp。
- Non Power of 2：None。
- sRGB：On。
- Alpha Is Transparency：On。

生产 Sprite 的画布必须与声明的行列和单帧尺寸完全一致。Scene-01i 世界角色帧为 `24×32 px`，脚底中心 Pivot 保持一致；物品图标为 `16×16 px`。所有 Sprite 名称使用稳定 ID 的资源形式，例如 `npc_shopkeeper_mina_idle_down`、`item_seed_potato` 和 `crop_tomato_stage_04`。

## 6. A1 视觉与动画验收

- 草地、道路、土地和水面以 `3×3` 重复铺设时没有可见接缝。
- 透明边缘没有白边、暗边、棋盘格背景或半透明噪点。
- 玩家、NPC、建筑门口、成熟作物和交互提示在相邻背景上保持清楚轮廓。
- 动画各帧的身体高度、头部大小、光向和脚底位置不跳动。
- 原生尺寸及 `2×`、`4×` 整数放大时像素边缘清楚；非整数缩放不作为发布显示方式。
- Unity 导入、场景加载和对应 EditMode/PlayMode 测试没有编译错误、丢失引用或运行时异常。

## 7. 红—绿循环

| 循环 | Red | Green | 后续验收 |
| --- | --- | --- | --- |
| Import-01 | 测试纹理仍以 Bilinear、压缩或错误 PPU 导入 | Production 目录的导入策略统一为 Point、无压缩、16 PPU、无 mipmap | Reviewer 检查测试只通过公开 Importer 结果 |
| Anchor-01 | A0 清单缺少一个或多个锚点 | 五个 A0 PNG 均存在且可导入 | 人工执行 ART-A0-01 至 ART-A0-08 |
| Probe-01 | A0 胡萝卜探针不存在，导入契约测试失败 | 源稿经确定性工具编译为 16×16、二值 Alpha、最多 8 色的 Single Sprite | Reviewer 检查 1×/4×可读性、光向和原创性 |
| Batch-01 | 11 个 Production PNG 均不存在，清单测试列出全部缺失项 | 声明式编译器生成 11 个 PNG、98 个 Sprite 与 11 个 4× 预览，清单测试通过 | Reviewer 按角色、世界、物品、状态和 UI 批次检查可读性 |
| Sprite-01 | 首个角色或 Tile Sheet 的尺寸、Pivot、切片或名称不符合清单 | 对应生产资源通过布局测试 | 在测试场景检查轮廓、接缝和脚底稳定性 |
| Scene-01 | 正式场景仍引用占位方块或缺少 Pixel Perfect Camera | 场景引用验收后的 Production 资源并使用整数像素显示 | PlayMode 验证移动、交互和 UI 不回归 |
| Player-01 | 主角旧帧高度为 `29..32`，右向脚部缺失，左右镜像轮廓低于门禁 | 12 个 `24×32` authored 单元通过包络、脚底与镜像轮廓测试 | 人工检查原生尺寸和整数倍画面中的体型与动作观感 |

先固定清单和可观察契约，再生成对应批次；不得在资源生成后补写更宽松的测试，也不得为了让测试通过而降低像素规格。

### 7.1 Scene-01 场景接线验收

Scene-01 按九个垂直切片验收，任一切片未通过时不得把正式场景标记为完成：

1. **Scene-01a 世界表现**
   - Main Camera 具有 URP `PixelPerfectCamera`，`assetsPPU=16`、参考分辨率 `320×180`，使用点采样整数放大。
   - 小镇地面与道路由 Tilemap 消费 `tile_town_base_16.png`；至少一个 `3×3` 草地重复区和相邻道路组合可见且无接缝。
   - 商店、住宅、厨房、鸡舍、农田、池塘、玩家和世界 NPC 引用对应 Production Sprite；这些对象不再引用 Unity 内置 `UISprite`。
   - 场景保持七种交互语义和 10 个 `TownInteractionPoint2D`；视觉对象不取得任何领域写接口。
2. **Scene-01b 运行时状态表现**
   - 玩家根据真实移动方向显示四方向静止帧，并在移动时循环对应两帧步行动画；停止后回到最后方向的静止帧。
   - 6 个农田位置把空地、浇水和既有作物成长进度映射到清单中的状态 Sprite。
   - 母鸡把未喂、已喂和产物可收映射到三个既有状态 Sprite；映射只读取现有 ViewState。
3. **Scene-01c UI 皮肤**
   - 正式界面使用 UGUI `Canvas`；`CanvasScaler` 的参考分辨率为 `320×180`。`OnGUI` 调试界面不得作为本切片的验收对象。
   - 正式 HUD、模态面板和按钮消费 `ui_panel` 与四个按钮状态的 `3 px` 九宫格资源；金币、时间、保存、读取、齿轮和交互气泡使用对应图标。
   - 文字由 Unity 字体渲染；在 `320×180` 参考分辨率及 `2×`、`4×` 整数倍窗口中不裁切、不重叠，按钮仍可操作。
4. **Scene-01d UI 信息架构**
   - Scene-01d 收口后，左下持续交互面板不存在；交互提示只在最近目标上方显示 `E` 气泡。左上只保留天数、时间和金币，右上只保留齿轮，底部中央显示五格快捷栏。
   - 包裹和系统菜单继续使用相同的近黑纯底与木框；系统菜单主页面只包含“保存游戏”“加载存档”“设置”“离开游戏”。
   - 包裹、系统菜单和既有业务模态共享输入门；任一界面关闭或被撤销后恢复原有玩家控制。
5. **Scene-01e 场景语义边界**
   - 四座建筑只能从门槽进入浅层区域并触发业务界面；农田与池塘按可见外沿阻挡玩家，池塘从可达岸边一圈保持钓鱼交互优先级。
   - Mina、Eli、Ren 和 Sora 分别使用稳定 ID、世界 Sprite、合法坐标和独立对话入口；一次交互只显示当前 NPC。
   - 系统菜单四个主按钮使用深木色与奶油色 10px 标签；右上只显示无底框灰色齿轮。
6. **Scene-01f 人工问题收口**
   - 七个业务模态位于快捷栏上层；按钮与快捷栏数字满足各自的颜色契约。
   - 房屋下方约 3/5 保持实体阻挡，上方约 2/5 可通行；母鸡和四名 NPC 位于合法、不重叠的位置。
7. **Scene-01g 屋后遮挡与角色辨识度**
   - 角色进入房屋背面通行区时被对应屋顶遮挡，不显示在屋顶表面。
   - 主角和四名 NPC 的 `24×32` 世界 Sprite 在原生尺寸及整数倍显示时具有完整身体轮廓；NPC 发型、帽子和职业服装与头像映射一致。
8. **Scene-01h 门槽进入深度**
   - 四座房屋的门槽深度统一为 `0.6` 世界单位；门宽、门口业务 Trigger、房屋侧墙阻挡和屋后通行区保持不变。
   - 玩家到达门槽最深可达位置时，完整角色轮廓仍位于屋顶前景最低不透明像素下方，并保留至少 `1 px` 原生像素间距。
   - 真实 `Rigidbody2D` 仍可进入每个门槽并触发对应业务入口，且不能穿过非门区域。
9. **Scene-01i 主角移动图集规格化**
   - 12 个 `ArtSource/Authored/A1/Characters/Player/*.pixels` 文件分别提供一个精确 `24×32` 单元；正式输出、Sprite 名称、`16 PPU`、BottomCenter Pivot 和 Animator 映射保持不变。
   - 每帧脚底为 `y=0`，顶部为 `y=29` 或 `y=30`，水平边界位于 `x=2..21` 内，中心位于 `x=11..12`；脚底至少有 2 个不透明像素，所有不透明像素通过四邻域连接到脚底，12 帧高度差不超过 1 像素。
   - 左右 idle、walkA、walkB 的整体水平镜像轮廓交并比均不低于 `0.9`，底部 8 行均不低于 `0.85`；人工继续检查体型、脚部和动作观感。

自动化停止线：EditMode 与 PlayMode 全量测试通过，Production UI 引用、文字值、按钮状态与事件、关键布局边界、主角帧包络、模态输入恢复和升级幂等均有通过记录，且场景无丢失引用或运行时异常。自动化通过不判定文字肉眼可读性、图标辨识、Tile 接缝、实际运动中的脚底稳定、动作观感或整体构图。

最终退出条件：人工在 `320×180`、`640×360`、`1280×720` 画面中完成 Tile 接缝、玩家脚底稳定、功能物件辨识、农田和母鸡状态、UI 可读性与按钮可操作性检查。Scene-01 人工验收通过前保持固定 NPC 对话和故障替身，不接入真实 AI 驱动 NPC，也不开始 AI NPC 评测。

## 8. 当前状态

| 项目 | 状态 |
| --- | --- |
| 纯像素方向与规格 | 已锁定 |
| A0 五张风格锚点 | 已生成；自动检查 2/2 通过，人工检查仍有软渐变问题 |
| A0 胡萝卜像素管线探针 | 自动检查 1/1 通过；人工复核通过 |
| A1 Production 清单测试 | 初始 Red 精确报告 11 个缺失文件；现行 Production 目标夹具 9/9 通过 |
| A1 可切片资源 | 13 个 PNG、106 个 Sprite、13 个 4× 预览已生成；包含独立齿轮、屋顶前景、四人世界图集和 12 个主角 authored 单元 |
| 正式场景替换 | Scene-01a/01b/01c/01d/01e/01f/01g/01h/01i 自动化已完成；最终人工场景验收待执行 |

## 9. A0 审查记录（2026-08-29）

| 编号 | 结果 | 证据 |
| --- | --- | --- |
| ART-A0-01 | 未通过 | 五张图仍包含连续渐变和软色阶，像素块大小不一致 |
| ART-A0-02 | 通过 | 小镇和环境保持 3/4 俯视，主要高光在左上方 |
| ART-A0-03 | 通过 | 商店、住宅/床、农田、鸡舍、池塘和厨房不依赖文字即可区分 |
| ART-A0-04 | 通过 | 玩家与 4 名 NPC 在发型、帽型、服装颜色和职业轮廓上可区分 |
| ART-A0-05 | 通过 | 未出现战斗、野外、季节、天气或新增交互系统 |
| ART-A0-06 | 未通过 | 城镇、人物、UI 和调色板的模拟像素密度、描边和材质处理存在批次漂移 |
| ART-A0-07 | 通过 | 视觉检查未发现现有游戏角色、Logo、地图或建筑的可识别复制；该结论不是法律审计 |
| ART-A0-08 | 未通过 | 镇景不是 `320×180` 的整数倍，人物和图标未提供真实 `16×24`、`16×16` 原生样张 |

五张图均为 RGB，不含 Alpha；人物与 UI 图中的棋盘格是烘焙背景。它们继续只作为构图、角色轮廓和暖色关系参考，不得缩小后直接复制到 `Production`。A1 使用独立源稿与确定性编译器解决真实原生尺寸、锁色和硬透明问题。

## 10. A0 胡萝卜像素探针记录（2026-08-29）

`a0_item_crop_carrot.png` 是 `crop.carrot` 的单图标管线探针。自动检查验证了源 PNG 与 Unity Sprite 的 `16×16 px` 尺寸、中心 Pivot、16 PPU、Point、无压缩、无 mipmap、Clamp、Full Rect、真实 Alpha、二值 Alpha 和最多 8 个不透明颜色。

人工复核结果：非透明像素 `67`，不透明颜色 `7`，Alpha 取值为 `0` 和 `255`，`64×64 px` 预览与 4 倍最近邻结果完全一致；1× 可读性、硬像素边缘、左上光向、色板关系和原创性均通过。本结论只批准该图标作为后续物品图标的像素语言基准；A1 使用独立清单和批次门禁。

## 11. A1 全套资源验收记录（2026-08-29）

- 范围：`docs/ART_ASSET_MANIFEST.md` 中 11 个 PNG、98 个命名 Sprite，未增加玩法对象。
- TDD RED：`Logs/A1ProductionManifestCurrentRedTests.xml` 为 1 failed，失败内容仅为 11 个 Production PNG 缺失。
- TDD GREEN：`Logs/A1ProductionManifestGreenTests.xml` 为 1 passed；A0 回归 1 passed。
- 第二轮 RED：道路连接、UI 九宫格 border 和玩家脚底基线 `3/3` 精确失败；修复后目标测试 `3/3` 通过。
- 全量回归：EditMode `158/158`、PlayMode `26/26` 通过；日志中未发现 C# 编译错误、测试失败、未处理异常或 Bootstrap 初始化失败。
- 自动门禁：真实 PNG 尺寸、二值 Alpha、全不透明 Tile、32 色成员、道路连接、BottomCenter 脚底基线、UI border、Sprite 类型、16 PPU、Point、无压缩、无 mipmap、Clamp、Full Rect、切片数量/名称/Rect/Pivot，以及 4× 最近邻预览均通过。
- 目视门禁：建筑、农田、池塘、人物方向、4 名 NPC、18 个物品、作物成长、母鸡三态和 12 个 UI 图标均可辨识；细节润色不阻断本批次。
- 本次资源批次验收当时未覆盖正式场景引用、Tilemap `3×3` 实铺视觉接缝、运行时动画播放和 Pixel Perfect Camera；Scene-01a/01b 的后续接线结果见第 12 节，最终人工接缝检查仍待完成。

## 12. Scene-01a/01b 接线记录（2026-08-29）

- Scene-01a：正式场景已使用 Tilemap、7 个 Production 功能点、Production 玩家与世界 NPC；Main Camera 使用 `assetsPPU=16`、`320×180` 的 URP Pixel Perfect Camera。
- Scene-01b：玩家根据真实 Rigidbody2D 速度和最后方向切换四方向 idle/walk；6 个农田位置使用独立土壤与作物叠层；母鸡显示 idle/fed/product-ready。
- 权限边界：农田和母鸡世界视图只接收 `FarmViewState` 与 `LivestockViewState`；未增加领域写接口、玩法对象或 AI 接口。
- TDD：Scene-01a RED 命中缺少 Pixel Perfect Camera；玩家、农田和母鸡 RED 分别命中静态 idle、缺少 Farm States 和缺少 Hen State。对应 GREEN 均进入正式场景测试。
- 自动回归：EditMode `159/159`、PlayMode `26/26` 通过；日志无 C# 编译错误、测试失败、未处理异常或 Bootstrap 初始化失败。
- 升级幂等：连续两次执行 A1 场景升级后，`CozyTown_Dev.unity` 的两次 SHA-256 比较结果相同。
- 未覆盖：目标分辨率文字与按钮人工检查、最终 Tile 接缝和整体构图人工检查；Scene-01c 自动化结果见第 13 节，因此 Scene-01 仍未完成人工验收。

## 13. Scene-01c 接线记录（2026-08-29）

- UGUI 基础：正式场景使用 `ScreenSpaceOverlay`、Pixel Perfect Canvas、`320×180` 参考分辨率的 `CanvasScaler`、`GraphicRaycaster` 和 Input System UI 模块。
- Production 消费：HUD、交互提示、保存/读取及 7 类模态消费 `ui_panel`、四个按钮状态、金币、时钟、保存、读取、关闭、选择和交互图标；18 个物品与 4 个 NPC 通过稳定 ID Catalog 映射到 Sprite。
- 界面行为：商店、农田、床、鸡舍、池塘、厨房和 NPC 固定回退界面均通过真实 UGUI Button 路由原有 Presenter/View 事件；退役的 IMGUI 路径已删除。
- TDD：各垂直切片先以缺失 Canvas 或 `ConfigureUi` 公开接缝形成精确 RED，再加入最小 GREEN。场景 PlayMode 用例通过真实按钮执行保存、读取、购买和关闭。
- 自动回归：EditMode `166/166`、PlayMode `27/27` 通过；日志未发现 C# 编译错误、测试失败或未处理异常。
- 布局与幂等：10 个正式面板通过三档同宽高比的静态参考坐标边界检查；该检查不驱动实际 Game View 分辨率，三档实际布局仍由人工验收。场景升级器连续运行前后的 `CozyTown_Dev.unity` SHA-256 相同。
- 停止线：实际文字可读性、图标辨识、按钮手感、Tile 接缝、玩家脚底稳定和整体构图仍由人工验收。通过前继续使用固定 NPC 对话和故障替身，不启用真实 AI 服务或 AI NPC 评测。

### 13.1 UI 面板透明中心修复（2026-08-30）

- 人工检查发现 HUD、存档和对话模态使用的 `ui_panel` 九宫格中心透明，导致世界场景透过面板显示。
- 资产契约测试先复现可拉伸中心的 64 个透明像素；A1 批次定义随后只将该单元的透明像素填充为 `#1F1B24FF`，并重建 Production PNG 与 4× 预览。
- 目标测试、Production 资源清单及全量 EditMode、PlayMode 回归均通过；其他 UI 单元、Production PNG、Meta 文件和正式场景未变化。
- 本修复关闭资源与生成管线缺陷；三档实际画面的面板遮挡、文字可读性和按钮可操作性仍由人工场景复查确认。

### 13.2 UI 面板纯净木框验收基准（2026-08-30）

视觉参考为 `ArtSource/Generated/A1/ui_clean_wood_panel_reference.png`。该图片只规定近黑面板表面、直线木框、统一方形转角和暖棕色关系；参考图中的额外图标、工具栏、状态条、排版和布局不进入 CozyTown 范围，也不得被场景直接引用。

Production `ui_panel` 使用以下确定性像素契约：

- 原生单元保持 `16×16 px`，Sprite 九宫格 border 保持 `(3,3,3,3)`。
- 距单元外缘第 `0`、`1`、`2` 层像素依次为 `#3B1F1BFF`、`#8A3B12FF`、`#C98256FF`；内部 `10×10 px` 全部为 `#1F1B24FF`。
- 四角与四条边只由上述三层连续木色构成，不包含绿色、米色补丁、棋盘拼块、花纹、绳结、钉饰或透明像素。
- 只允许重建 `ui_panel` 单元及其 4× 预览对应区域；其余 11 个 UI 单元、另外 10 张 Production PNG、Meta 文件、Sprite 名称、场景引用和界面布局保持不变。
- 自动化通过公开 Production PNG、`Sprite.border` 与 `ImageConversion.LoadImage` 验证，不读取 `.meta` 文本或私有生成器实现。
- `320×180`、`640×360`、`1280×720` 中的边框观感、文字可读性和按钮可操作性继续由人工场景复查确认。

自动化结果：木框契约在修复前产生 1 failed，重建后目标测试 `1/1`、Production 清单 `5/5`、全量 EditMode `169/169`、PlayMode `27/27` 通过。连续两次 A1 Build 的 22 个 Production 与预览输出哈希一致；相对上一版本，仅 `ui_panel` 单元的 160 个像素及 4× 预览对应区域的 2560 个像素变化，允许区域外变化为 0。Meta、场景、其他 UI 单元、另外 10 张 Production PNG 和另外 10 张预览均未变化。

### 13.3 Scene-01d 常驻 UI 接线记录（2026-08-30）

- 常驻区域收敛为左上日期、时间和金币，右上 `16×16` 齿轮，以及底部中央五格快捷栏；旧左下交互面板和右上常驻存档面板已从场景退役。
- 最近交互目标通过公开锚点显示 `E` 气泡；包裹和快捷栏只消费背包只读投影，不提供使用、装备、拖放、丢弃或排序写操作。
- `B` 打开、关闭包裹，数字 `1` 至 `5` 只切换快捷栏选中格。齿轮系统菜单只包含保存游戏、加载存档、设置和离开游戏；设置页保持可返回的范围占位页。
- 新增 `ui_icon_settings` 独立 Production PNG，并只修改 `ui_marker_interact` 的目标单元。当前 A1 总计 12 个 Production PNG、99 个命名 Sprite 和 12 个 4× 预览。
- 系统菜单、包裹和既有业务模态共享输入门；测试覆盖互斥、关闭恢复、输入门撤销、组件停用恢复，以及真实场景 Keyboard 输入链路。
- 场景级 EditMode `5/5`、PlayMode `2/2`，全量 EditMode `179/179`、PlayMode `31/31` 通过。场景升级器连续两次运行后的 SHA-256 均为 `6A3420F637BA43A8A0F30EB8411C9008107DA737EE031E1379A595112AD4287F`。
- 停止线保持不变：三档实际画面的 UI 可读性、遮挡、按钮手感、Tile 接缝、玩家脚底稳定和整体构图仍由人工验收；通过前不接入真实 AI 驱动 NPC。

### 13.4 Scene-01e 场景语义边界记录（2026-08-31）

- A1 新增一张四格 `16×24` 世界 NPC 图集及对应 4× 预览；当前总计 13 个 Production PNG、103 个命名 Sprite 和 13 个预览。
- Mina、Eli、Ren 和 Sora 分别使用稳定 ID、独立世界 Sprite、不同合法坐标和对应 Presenter；一次交互只显示当前 NPC。
- 四座建筑使用保留浅门槽的实体多边形，业务 Trigger 只位于门口；农田和池塘使用接近可见外沿的实体边界，池塘 Trigger 允许从岸边一圈探测。
- 系统菜单主按钮使用深木色底与奶油色 10px 标签；右上入口只显示无底框灰色齿轮。
- 全量 EditMode `183/183`、PlayMode `35/35` 通过；A1 世界与 UI 升级入口连续运行后的场景 SHA-256 一致。
- 停止线保持不变：三档实际画面的文字辨识、按钮手感、门口进入深度、池塘岸线贴合、NPC 构图、Tile 接缝和整体观感仍由人工验收；通过前不接入真实 AI 驱动 NPC。

### 13.5 Scene-01f 人工问题自动化收口记录（2026-08-31）

- 商店、农田、床、鸡舍、池塘、厨房和 NPC 模态在 Canvas 中位于快捷栏之后；这些模态的按钮使用深木色 `#6F5A4A` 与奶油色 `#FFF4D6`，快捷栏 `1` 至 `5` 使用深棕色 `#3B1F1B`。
- 四栋房屋的实体高度从完整 4.0 世界单位收敛为下方 2.4；上方约 2/5 可通行，门槽、非门墙阻挡和门口业务 Trigger 保持原有范围。本阶段不实现角色位于屋顶后的透视遮挡。
- 母鸡移动到农田右侧草地，距农田实体边界约 0.6 世界单位，且不与六个实体障碍重叠。
- 四个 `16×24` authored 世界 Sprite 保留与头像一致的特征：Mina 棕发围裙、Eli 草帽绿衣、Ren 蓝帽蓝衣、Sora 白厨师帽橙发。稳定 ID、Sprite 名称、图集路径和头像路径均有场景测试覆盖。
- 全量 EditMode `188/188`、PlayMode `35/35` 通过；场景升级入口重复执行及主工作区/隔离场景的 SHA-256 均一致。
- 停止线保持不变：三档实际画面的按钮文字、遮挡、房屋背面观感、NPC 构图、母鸡位置和整体画面仍由人工验收。

### 13.6 Scene-01g 屋后遮挡与角色辨识度记录（2026-09-01）

- 四座房屋增加只保留顶部 26 行像素的前景 Sprite。完整建筑底图位于角色后方，屋顶前景位于角色前方；碰撞体、门槽和业务 Trigger 未改变。
- 主角移动图集与四名 NPC 世界图集升级为 `24×32`。四名 NPC 的稳定 ID、Sprite 名、头像路径和 Presenter 映射保持不变。
- RED 分别命中缺少屋顶前景、NPC 世界形象不足以保留头像特征，以及主角仍引用 `16×24` 图集。GREEN 后全量 EditMode `190/190`、PlayMode `35/35` 通过，均为 0 failed、0 skipped。
- A1 批次和场景升级入口重复执行前后，场景、屋顶、NPC 和主角输出的 SHA-256 分别保持一致。
- 停止线保持不变：人工仍需在三档目标分辨率检查角色比例、四座房屋背面遮挡、脚底基线、NPC 辨识度和整体构图；通过前不接入真实 AI 驱动 NPC。

### 13.7 Scene-01h 门槽进入深度记录（2026-09-01）

- 可观察问题：玩家进入左下住宅门槽最深位置时，头部会进入屋顶前景的不透明区域，造成角色被遮挡。
- GREEN 将商店、鸡舍、厨房和住宅的门槽深度从 `1.25` 或 `1.30` 世界单位统一收敛为 `0.6`；门宽、门口业务 Trigger、非门墙体和屋顶排序未改变。
- RED 使用正式场景碰撞体、玩家 `24×32` Sprite 边界和屋顶前景不透明像素计算最深可达位置。商店样本的玩家头顶为 `3.95`，允许上限为 `3.3125`；GREEN 后目标 EditMode `1/1` 和真实 `Rigidbody2D` PlayMode `1/1` 通过。
- 全量 EditMode `191/191`、PlayMode `35/35` 通过，均为 0 failed、0 skipped；场景升级入口连续执行前后的场景 SHA-256 均为 `B46F9F6E41E99E131D69798CA7E3CC2B425A56CF6A3F2FB23B525446FB20DB27`。
- 自动化证明最深可达位置保留至少 `1 px` 的原生像素间距，并保留门口进入与交互行为；三档实际画面中的进入手感和头部留白仍由人工验收。

### 13.8 Scene-01i 主角移动图集规格化记录（2026-09-01）

- 可观察问题：旧图集逐格自动裁边和缩放后，12 帧可见高度为 `29..32`；右向帧出现脚部缺失，移动时角色体型不稳定。
- RED 的左右 idle、walkA、walkB 镜像轮廓交并比分别为 `0.791`、`0.639`、`0.740`，均低于 `0.9` 门禁。
- GREEN 使用 `ArtSource/Authored/A1/Characters/Player/` 下 12 个精确 `24×32` `.pixels` 单元覆盖图集。正式 Production 路径、12 个 Sprite 名、`16 PPU`、BottomCenter Pivot 和 Animator 映射未改变。
- Production 目标夹具 `9/9`、全量 EditMode `193/193`、PlayMode `35/35` 通过，均为 0 failed、0 skipped。连续 A1 重建前后的主角 Production PNG、4× 预览和当前场景 SHA-256 分别保持为 `6B3FDBE287AB23372CDAEBAAE41CAE645B9B4548EE2A2F15B175E7E9C1C13D90`、`1717079B2C7AE1D716959E45533788D86AFD9A508024CCE48F62DC73312E1604`、`903D7E53BCFB4D9244939F3F08EBF4C2499FB352520C06CB29AA9C2AB3BBEECC`。
- 自动化证明帧尺寸、包络、脚底连续性和左右轮廓满足门禁；三档实际画面中的角色体型、脚部完整性和动作观感仍由人工验收。Scene-01 通过前不接入真实 AI 驱动 NPC，也不推进 M5。

## 14. 可延期润色项

- 道路连接已满足功能契约，但细节密度低于草地；后续可在不改变 6 px 边缘签名的前提下增加纹理。
- 玩家动作的像素细节可在不改变 Scene-01i 包络、镜像轮廓和稳定 Sprite 契约的前提下继续润色；当前仍需人工确认动作观感。
- 已喂母鸡、可收蛋、干地和湿地状态可以增加色相或轮廓差异；当前原生尺寸可辨识。
- 重建前后文件哈希、Catalog 文件拆分和 Unity 新版 Sprite 数据接口属于管线强化，不阻断本批次或正式场景接线。
- 四格世界 NPC 图集已由四个 `.pixels` 单元完全覆盖，但批处理定义仍要求一个未参与像素输出的旧头像 source 文件；移除该虚假依赖需要调整通用编译器契约，作为非阻塞资源管线债务处理。

## 15. T1 / Town-01 验收补充（人工待执行）

T1 的资源与运行验收以 [扩镇与居民日常规划](TOWN_LIFE_PLAN.md) 为准；资源接线和自动化结果见第 15.1 节，以下人工结果尚未登记。

1. 记录构建/提交、分辨率、日期和操作者。分别检查 `320×180`、`640×360`、`1280×720` 的相机跟随、地图四边、文字、HUD 与像素稳定性。
2. 逐一辨认四户住宅的外观与归属；玩家住宅保留，所有出入口、住宅支路和职业地点可达。
3. 观察四人正常一天：从对应住宅出发、沿路前往职业地点、午间活动、按各自返家窗口归家。个人时刻见 [实现记录](TOWN_LIFE_IMPLEMENTATION.md#production-routine-configuration)。不能穿墙/水/田、用正面图滑行、远离住宅突然消失或到家后留下隐形交互。
4. 逐一打开移动中 NPC 的对话，检查暂停、身份/头像一致、关闭续行和气泡跟随；检查人物相交排序、屋后遮挡和浅门槽头顶净空。
5. 观察白天、通勤和夜间读档，按规划中的合法归位策略评估，不将位置变化误写为精确续走。失焦、模态等待和失败加载不产生残留运动或未授权跳时。
6. 在扩大版上复核 Scene-01 的原生人物帧、Tile 接缝、池岸、农田/母鸡状态、深木色 UI 以及生产经济闭环；Scene-01 与 Town-01 分别记录通过/失败和遗留项。

上述联合门禁通过前，不接入真实 AI 驱动 NPC，不把自动化结果当作人工结果。可以在同一次人工验收会话完成两份检查，不必先验收被替换的旧地图。

### 15.1 自动化准备状态（2026-09-06）

四人 48 个 `24×32` 帧、四住宅及对应屋顶已接入正式场景。独立 T1 编译批次新增 6 张 Production PNG、56 个 Sprite；Production 目录合计保留 19 张 PNG、162 个 Sprite。15 条 T1 资源测试及既有角色身份、颜色、脚底和尺寸契约通过；共享 Y 排序、门槽净空和按有效时间播放动画的图形回归通过。

最终 EditMode `450/450`、PlayMode `115/115`，证据为 `Logs/npc-final-editmode03.xml`、`Logs/npc-final-playmode03.xml`。实际相机截图记录于 [T1 实现记录](TOWN_LIFE_IMPLEMENTATION.md#actual-scene-camera-evidence)，主代理已检查住宅街出门及职业地点画面。上述结果没有记录用户对角色辨识度、行走节奏、三档分辨率或联合场景的人工确认，#33 继续开放。
